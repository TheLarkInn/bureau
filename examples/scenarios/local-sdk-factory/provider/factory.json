{
  "name": "review",
  "description": "Review the Bureau work item read-only, then independently verify the assessment. Args may be null or an object with an optional focus string.",
  "phases": [
    { "title": "Review" },
    { "title": "Verify" }
  ],
  "argsSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": ["object", "null"],
    "properties": {
      "focus": { "type": "string", "minLength": 1, "maxLength": 1000 }
    },
    "additionalProperties": false
  }
}
